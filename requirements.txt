fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.20
pydantic>=2.10
httpx>=0.28

# Motor OCR (VISION_BACKEND=ocr, el que viene por defecto). Los modelos ONNX
# van dentro del paquete: el contenedor no descarga nada al arrancar.
# opencv NO se lista aqui a proposito: lo arrastra rapidocr como
# opencv-python, y el Dockerfile lo sustituye despues por la variante headless.
# Listar headless aqui hacia que se instalaran los dos, y como comparten el
# directorio cv2, desinstalar uno borraba los ficheros del otro.
# Version fijada, no por capricho: la 1.4 reestructuro la API interna y dejo de
# exponer text_detector/text_recognizer, que son justo las piezas que usa
# app/vision/ocr.py para separar deteccion y reconocimiento. La 1.2.3 es la que
# esta medida al 100% sobre el fixture.
rapidocr-onnxruntime==1.2.3
numpy>=1.26
pillow>=10.0

# Solo hace falta con VISION_BACKEND=claude.
anthropic>=0.40
