fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.20
pydantic>=2.10
httpx>=0.28

# OCR engine (VISION_BACKEND=ocr, the default). The ONNX models ship inside the
# package, so the container downloads nothing at startup.
#
# The version is pinned deliberately: 1.4 reorganised the internal API and
# stopped exposing text_detector / text_recognizer, which are the two pieces
# app/vision/ocr.py drives separately to narrow down first and read afterwards.
rapidocr-onnxruntime==1.2.3
numpy>=1.26
pillow>=10.0

# opencv is NOT listed here on purpose: rapidocr pulls it in as opencv-python,
# and the Dockerfile then swaps it for the headless variant. Listing headless
# here caused both to be installed, and since they share the cv2 directory,
# uninstalling one deleted the other's files.

# Only needed with VISION_BACKEND=claude.
anthropic>=0.40
